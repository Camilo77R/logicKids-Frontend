# Routes

Aquí vive la navegación principal del frontend.

Responsabilidades:

- definir qué pantalla corresponde a cada URL;
- decidir quién entra a rutas públicas o privadas;
- redirigir cuando haga falta.

Archivos clave:

- `AppRouter`
- `ProtectedRoute`
- `PublicRoute`
